/**
 * Debouncing
 * When a DOM events occurs repetatively and we need to limit it with some timeframe, 
 * debouncing is used.
 * Limit the frequent firing events.
 * Eg 1 - Search bar making API calls, and returning data.
 * Eg 2 - If user  presses Purchase button multiple times unknowingly, we should be able 
 *        to prevent it.
 * 
 * Events
 * scroll -> window.onscroll = ()=>{}
 */